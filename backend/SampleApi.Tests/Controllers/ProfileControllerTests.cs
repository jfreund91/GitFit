using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Net;
using System.Text;

namespace SampleApi.Tests.Controllers
{
    [TestClass]
    public class ProfileControllerTests
    {
        //[TestMethod]
        //public GetProfile_ShouldReturnCorrectValues()
        //{
        //    // ARRANGE

        //    // Create a webclient so that we can login and get a token
        //    WebClient wc = new WebClient();

        //    // Login: Call AccountController.Login, and save the token. This is a POST to Login 

        //    // Use the WebClient to execute the method we want to test. Pass the saved token in the header

        //    // Parse the result to see if we got what we expect.

        //}
    }
}
